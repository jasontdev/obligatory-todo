{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$user_id": {
        ".write": "$user_id === auth.uid",
        ".read": "$user_id === auth.uid",
        "items": {
          ".indexOn": "index"
        }
      }
    }
  }
}
